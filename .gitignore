# build/runtime cruft — none of this needs to be tracked
node_modules/
*.log
.DS_Store

# The single-file app is regenerated by `node build.js` / the Docker build stage.
# Keep it ignored so the repo has one source of truth (the source files).
CT-Driver-Practice-Test.html

# local dev only
public/
.localdata/
